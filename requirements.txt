# =============================================================================
# DataBrief AI — requirements.txt
# Versões pinadas para reprodutibilidade no Google Colab gratuito (Python 3.10)
# Última revisão: Ato 2 (29/08/2026) — ver notas de mudança no fim do arquivo
# =============================================================================
# ── Orquestração do grafo de agentes ─────────────────────────────────────────
langgraph==0.2.73           # grafo de estados para o agente conversacional
langchain-core==0.3.62      # abstrações base usadas pelo LangGraph
langchain==0.3.25           # graph/agent.py importa langchain diretamente (não só
                             # langchain-core) — funcionava sem estar pinado porque
                             # o Colab já traz pré-instalado, mas não é garantido
# ── Modelos de linguagem (LLM / SLM) ─────────────────────────────────────────
transformers==4.51.3        # carregamento de Qwen3 e Whisper via HuggingFace
accelerate==1.6.0           # distribui o modelo entre GPU e RAM automaticamente
bitsandbytes>=0.46.1        # quantização 4-bit (INT4) do Qwen — reduz VRAM de ~14GB para ~3.5GB
sentencepiece==0.2.0        # tokenizador usado por vários modelos HuggingFace
# ── ASR — Reconhecimento de fala (Ato 2) ─────────────────────────────────────
# Whisper Small/Base via transformers (AutoModelForSpeechSeq2Seq/AutoProcessor/
# pipeline em audio/transcricao.py) — não usa o pacote openai-whisper separado.
librosa                     # carregamento/processamento do áudio de entrada
soundfile                   # leitura/escrita de arquivos de áudio
# ── Anexos — PDF/DOCX/TXT (Ato 2, Bloco 06) ──────────────────────────────────
pdfplumber                  # extração de texto de PDF, por página
python-docx                 # extração de texto de DOCX, por parágrafo
# ── TTS — Síntese de voz pt-BR (Ato 2, Bloco 06) ─────────────────────────────
piper-tts                   # Piper TTS — pacote pip de verdade (audio/sintese.py),
                             # não binário separado como uma nota antiga aqui dizia
# ── Schemas e validação de dados ─────────────────────────────────────────────
pydantic==2.11.4            # validação dos schemas centrais (DemandState, etc.)
# ── Interface gráfica ─────────────────────────────────────────────────────────
gradio==5.29.1              # interface Blocks com abas, painel de estado e áudio
# ── Integração com OpenAI (modo alternativo, opcional) ───────────────────────
openai==1.82.0              # cliente para gpt-4o-mini no modo OPENAI — só usado se
                             # a PUC disponibilizar chave; parametrizável via
                             # MODO_EXECUCAO, nunca fallback silencioso
# ── Utilitários gerais ────────────────────────────────────────────────────────
torch==2.6.0                # PyTorch — base para todos os modelos (não reinstalar se já presente no Colab)
numpy==1.26.4                # operações numéricas — testado sem conflito mesmo após remover faiss-cpu
pandas==2.2.3                # manipulação de dados tabulares (logs, métricas)
python-dotenv==1.1.0        # leitura de variáveis de ambiente do arquivo .env local
# ── Persistência e I/O ────────────────────────────────────────────────────────
# google-colab já inclui google-auth e google-drive; não reinstalar
# Para Drive: usar google.colab.drive.mount() — já disponível no Colab
# ── Avaliação e métricas (Ato 3) ─────────────────────────────────────────────
scikit-learn==1.6.1         # F1, precision, recall para avaliação dos campos
# =============================================================================
# NOTAS DE INSTALAÇÃO NO COLAB
# =============================================================================
# Instalar com:
#   !pip install -q -r requirements.txt
#
# torch já vem pré-instalado no Colab — se a versão acima conflitar, use:
#   !pip install -q -r requirements.txt --ignore-installed torch
#
# bitsandbytes requer CUDA — em modo CPU_LOCAL, não é usado.
#
# Lista completa testada com pip resolver real (dry-run) sem conflitos de
# versão em 29/08/2026 — todos os 19 pacotes acima resolvidos juntos.
# =============================================================================
# NOTAS DE MUDANÇA (Ato 1 → Ato 2)
# =============================================================================
# Removidos: sentence-transformers, faiss-cpu — dependências do RAG, que foi
#   implementado, testado com corpus real e removido conscientemente do
#   projeto (ver claude/ato2_decisoes.md para a decisão e evidência).
# Removido: openai-whisper — nunca foi o que o código realmente usa; o ASR é
#   via transformers (ver seção ASR acima). Linha estava desde o Ato 1 mas
#   não refletia a implementação real.
# Corrigido: bitsandbytes==0.45.5 → bitsandbytes>=0.46.1, pra bater com a
#   versão que a Célula 1 do notebook realmente pede.
# Adicionados: langchain, piper-tts, pdfplumber, python-docx, librosa,
#   soundfile — todos entraram no Bloco 06 (anexos + TTS) e no fix do
#   langchain faltando, nenhum estava registrado aqui ainda.
# =============================================================================
